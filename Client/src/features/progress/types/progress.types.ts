// features/progress — types = TypeScript structures that belong to the progress feature

// ProgressPeriod describes the time window for analytics data.
export type ProgressPeriod = "week" | "month" | "year" | "all_time";
