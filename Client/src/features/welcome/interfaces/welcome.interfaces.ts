// features/welcome — interfaces = TypeScript structures that belong to the welcome feature

// No custom interfaces yet — WelcomePage receives simple callback props.
// Add interfaces here when connecting to a backend (e.g. landing page config from CMS).
export type {};
