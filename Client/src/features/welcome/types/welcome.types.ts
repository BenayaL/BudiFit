// features/welcome — types = TypeScript structures that belong to the welcome feature

// No custom types yet — WelcomePage is a simple public landing screen.
// Add welcome-specific types here when the feature grows (e.g. CTA variants, A/B test flags).
export type {};
